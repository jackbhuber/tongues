# COUNT TOTAL MISSING VALUES IN EACH COLUMN OF DATA FRAME
# sapply(items, function(x) sum(is.na(x)))

# COUNT TOTAL MISSING VALUES FROM THE ENTIRE DATA FRAME
# sum(is.na(items))

# SUM ROWS INTO A NEW COLUMN (I.E. TOTAL SCORE)
# data$row_sum <- rowSums(data[ , c(1,3)], na.rm=TRUE)

